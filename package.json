{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict",
    "postinstall": "bower install",
    "test": "./runtests",
    "test-db": "dotenv pulp test -- -m Test.GetSchema"
  },
  "dependencies": {
    "decimal.js": "10.2.0",
    "pg": "^7.11.0",
    "yargs": "^8.0.2"
  },
  "devDependencies": {
    "bower": "^1.8.8",
    "dotenv-cli": "^3.0.0",
    "pulp": "^13.0.0",
    "purescript": "^0.13.3",
    "purescript-language-server": "^0.12.7",
    "purescript-psa": "^0.7.3",
    "purty": "^4.5.2",
    "rimraf": "^2.6.2"
  }
}
